# NOTES — canal de communication Claude ↔ Grok Bot

Ce dépôt est le terrain commun. Lucas ne relaie plus les fichiers à la main.

## Comment on se parle
- **Grok Bot** travaille dans Cursor sur ce dépôt, commit et push sur `main`.
- **Claude** lit le dépôt (clone public). Il **ne peut pas pousser** : son environnement bloque les accès GitHub authentifiés. Il rend ses retours à Lucas, qui les commit.
- Les messages passent par des fichiers, pas par les Issues (Claude ne peut pas les lire) :
  - `NOTES-GROK.md` → ce que Grok Bot a fait, ce qu'il a changé, ses questions. **À créer et tenir à jour par Grok Bot.**
  - `NOTES-CLAUDE.md` → ce fichier : retours de relecture de Claude.
- **Messages courts et datés.** Une ligne par point. On ne raconte pas, on liste.

## Règles de travail sur ce dépôt
1. **Un seul fichier applicatif : `index.html`.** Pas de découpage en plusieurs fichiers, pas de build, pas de `npm install`. Lucas doit pouvoir l'ouvrir et le modifier lui-même.
2. **Commits petits et décrits en français.** Un commit = un changement compréhensible. Pas de commit fourre-tout de 400 lignes.
3. **Ne jamais commiter de secret.** La clé Supabase présente dans le fichier est la clé *publishable*, publique par nature. La clé `service_role` ne doit jamais entrer dans ce dépôt, sous aucune forme.
4. **Ne pas toucher aux formules de calcul** (BMR, facteur d'activité, macros) sans accord écrit de Lucas dans ce fichier.
5. Le détail complet — architecture, schéma Supabase, pièges — est dans `BRIEFHANDOVERPLATEFORMEMHX.md`. **Le lire en entier avant de coder.**

## État au moment de l'ouverture du dépôt
- `index.html` poussé ici est **identique** à la version en ligne sur https://lcsmhxcoaching.netlify.app — empreinte vérifiée.
- Réglages Supabase faits par Lucas : `Confirm email` désactivé, Site URL et Redirect URLs correctes.
- Chantier principal restant : **suivi transversal de l'évolution** (voir §5 du brief). Rien d'autre n'est engagé.

## Retours de Claude
_(rien pour l'instant — première relecture après le premier push de Grok Bot)_

## 2026-09-25 — Refonte premium en 20 phases : Phase 2 livrée (index.html v33)
Contexte : Lucas a validé un plan de refonte « plateforme de coaching premium » en 20 phases (audit fait, design system, navigation, accueil, programme, nutrition, progression, suivi, bilan hebdo, feedback coach, objectifs, régularité, photos, formation, FREE/CLIENT, locked states + Calendly, back-office, fiche client, alertes, tests). Claude code les phases dans `index.html`, une par version, après validation de Lucas à chaque fois.

Règles absolues posées par Lucas (à respecter aussi côté Grok Bot) :
- ZÉRO PERTE DE DONNÉES : 7 clients + 1 coach réels dans Supabase. Aucune donnée existante supprimée, réinitialisée, écrasée ou rendue inaccessible.
- Avant toute migration BDD : backup vérifiable + comptage avant/après ; migrations non destructives uniquement (ADD COLUMN, CREATE POLICY, backfill explicite) ; jamais DROP / DELETE / TRUNCATE ; ancien format toujours lu.
- Phases sensibles (10 feedback coach, 13 photos, 15 FREE/CLIENT, 18 fiche client + notes coach) : brief de migration → validation Lucas → backup → migration → tests → vérification.
- Rien n'est poussé sur GitHub ni écrit en base sans le « oui » de Lucas. Réutiliser l'existant (Regularite, Journal, Historique, questionnaire, générateur, éditeur…), ne pas recréer.

Phase 2 (v33, ce commit) — visuel uniquement, aucune requête Supabase modifiée, aucune migration :
- Section [A] : sombre par défaut, clair en option (bouton ☀/☾, clé locale `mhx_theme`), palette premium, doré en accent seulement.
- Sections [B][C] : composants unifiés — stat card (`.tile` = `.tuile`), barre (`.bar` = `.jauge` = `.fo-jauge`), badges `.pastille.ok/.attention/.mauvais/.accent`, boutons `.btn/.ghost/.danger/.petit`, `.avatar`, `.timeline`, `.objectif`, `.switch`, `.verrou` (locked state), `.modale`, `.volet`, `.toast`. Anciens noms de classes conservés.
- Section [E] : objets `Theme` et `UI` (`UI.confirmer`, `UI.demander`, `UI.alerte`, `UI.toast`, `UI.volet`, `UI.verrou`). Les 29 `alert/confirm/prompt` natifs sont remplacés : **ne plus utiliser les dialogues natifs dans index.html**, passer par `await UI.confirmer(...)` etc.
- 19 entrées `I18N.en` ajoutées ; toute nouvelle chaîne visible doit avoir la sienne.
- Tests : 50 pages rendues (client, coach, fiche client) mobile/desktop, sombre/clair sur copie locale avec Supabase simulé, 0 erreur console ; 19 tests de flux (fenêtres, thème, anglais) OK.

Pour Grok Bot : rien à faire sur `index.html` (règle inchangée : Grok travaille dans `donnees/`). Si tu testes la v33 en ligne, note tes retours QA ici-même ou dans `NOTES-GROK.md` (thème, lisibilité, boutons au doigt, fenêtres). Prochaine étape : Phase 3 (navigation) après validation de Lucas.

## 2026-09-25 — Phases 3 et 4 livrées (index.html v34)
- Phase 3, navigation : ordre client Accueil · Mon programme · Nutrition · Ma progression · Mon bilan · Speed Formation · Mes compléments · Profil (ids/adresses inchangés ; alias `#/progression`, `#/suivi`, `#/repas`). Icônes SVG (`ICONES`) à la place des emojis. Sur téléphone : barre d'onglets en bas (4 onglets + « Plus » qui ouvre un volet). Le coach arrive sur « Mes clients » ; l'Accueil du client lui est visible en consultant une fiche (« Vue d'ensemble »). Le bloc « Mes données » (export / restauration / suppression) n'apparaît plus qu'au bas du Profil. Bannière PWA déplacée sous le contenu.
- Phase 4, accueil : nouvel outil `outilAccueil` (lecture seule) — « Bonjour Prénom » + résumé de la semaine, tuiles Régularité / Poids / Entraînement / Nutrition / Objectifs, « Aujourd'hui » (prochaine séance à noter, repas du jour, mesure de la semaine), objectifs du mois avec statut, lien vers le bilan. Réutilise `Regularite.calculer`, le journal, `repas_suivi.hist`, `mens`, `P.objectifs` + `objectifs_faits` : aucune nouvelle clé, aucune écriture.
- `Store.lireTout(cles)` : plusieurs clés en une requête (`outil=in.(...)`, lecture seule). Seule requête ajoutée.
- Tests : 58 pages rendues + 19 tests de flux + 13 vérifications de navigation (copie locale, Supabase simulé), 0 erreur.

## 2026-09-25 — Phases 5 à 8 livrées (index.html v35)
- Phase 5, programme : carte de tête « Cycle 01 · Semaine 2 sur 4 » (calculée depuis `P.debut`, posé à l'envoi ; le coach règle `P.cycle` et `P.duree_semaines` dans « Réglages du programme », défauts 1 et 4), compteur de séances notées cette semaine, cartes de séance avec badge « notée le … » / « à faire ». Éditeur coach intact. Le panneau « Ta semaine » a quitté le programme : il vit dans « Mon suivi » et l'Accueil.
- Phase 6, nutrition : bouton « Respecté / Non respecté » par repas (même clé `repas_suivi.mange`, même `Regularite.noterRepas`), compteur « x / y repas respectés » du jour, badge « aujourd'hui », préparation repliée côté client. Générateur et vue coach intacts.
- Phase 7, progression : page réorganisée — Poids (actuel, départ, variation, semaines, courbe) → Nouvelle mesure (repliée derrière « Ajouter ma mesure ») → Mensurations (cm perdus, courbes 4 max) → Composition corporelle → tableau → réglages (point de départ, zones). Tous les ids conservés, aucune logique modifiée.
- Phase 8, Mon suivi : nouvel outil `outilSuivi` = `Regularite.monterClient` (enrichi : évolution sur 5 semaines, objectifs en cartes à cocher — même clé `objectifs_faits`) + bilan des 28 jours (`outilBilan.calculer/vue`). « Mon bilan » sort de la barre (`masque_nav`) mais reste à `#/bilan` pour « Préparer le call ».
- Aucune nouvelle clé de données, aucune requête nouvelle, aucune migration. Tests : 62 pages, 19 flux, 27 vérifications ciblées — 0 erreur.

## 2026-09-25 — Phases 9, 11, 12, 14 livrées (index.html v36)
- Phase 9, bilan hebdomadaire : questions dans `CONFIG.bilan.questions` (modifiables sans toucher au code), objet `Checkin`, nouvelle clé **client** `checkins` = `{ liste: [{ semaine (lundi), fin, envoye_le, reponses{} }] }` (60 max). Disponible du vendredi (`CONFIG.bilan.jour_ouverture`) au dimanche suivant, jamais bloquant. Formulaire et historique dans « Mon suivi », statut + CTA sur l'Accueil, lecture coach dans « Préparer le call » (« Ses bilans hebdomadaires ») et « Son suivi ». Le feedback du coach (phase 10) s'accrochera à `semaine`.
- Phase 11, objectifs : le coach pose un statut par objectif dans « Son programme » (`P.objectifs.statuts[i]` = "" | atteint | non_atteint — champ **ajouté**, `liste[]` et `objectifs_faits` inchangés et toujours lus). Règle `Regularite.statutObjectif` : le statut du coach l'emporte quand il tranche, sinon la case du client. Badges En cours / Atteint / Non atteint partout (Mon suivi, Accueil, bilan, points à aborder).
- Phase 12, régularité : points et seuils dans `CONFIG.regularite` (`poids`, `seuils`) ; `Regularite.poids` / `niveau` les lisent. Formule inchangée par défaut (50 / 30 / 20 ; 70 / 40).
- Phase 14, formation : en-tête « Bibliothèque · 7 modules » avec tuile de progression, modules en cartes numérotées 00–06 avec objectif et barre par module ; contenu, vidéos, leçons, widgets et cases inchangés (`FORMATION`, `LECONS`, clé `formation`).
- Aucune migration : `checkins` est une clé client ordinaire (RLS propriétaire déjà en place). Tests : 62 pages, 19 flux, 40 vérifications ciblées — 0 erreur.

## 2026-09-25 — Phases 17, 19 et 18 (partie sans base) livrées (index.html v37)
- Module `Clients` : chargement partagé (profils + `donnees` sans historiques — les deux requêtes qu'utilisait déjà « Mes clients »), `resumer()` (le résumé par client et le tri par urgence, sortis de `outilClients.tableau` sans changement de logique) et `alertes()`.
- Phase 19, alertes (`Clients.alertes`) : calculateur ≠ questionnaire · jamais rien saisi / inactif ≥ 10 j · questionnaire / programme / diète manquants · régularité < seuil · bilan hebdo reçu (7 derniers jours, à lire) · bilan hebdo non complété (semaine visée passée) · objectif du mois « non atteint » · poids stable ≥ 21 j (objectif perte/prise). Chaque alerte pointe vers l'onglet de la fiche à ouvrir. Un point vert/orange/rouge par ligne dans « Mes clients ».
- Phase 17, `outilTableau` (page d'arrivée du coach) : Clients actifs · Prospects (`profils.statut = prospect`, 0 tant que la phase 15 n'est pas faite) · Bilans à traiter · Bilans non complétés · À surveiller ; « Qui nécessite ton attention aujourd'hui ? » (cartes par client, alertes cliquables) ; raccourcis. Barre du bas coach : Tableau de bord · Mes clients · Mes séances · Bibliothèque + Plus. « Mes clients » (tableau + comptes + création) inchangé, mais le tableau passe en cartes sur téléphone.
- Phase 18 (sans base) : « Fiche » = `outilAccueil.fiche()` en consultation — alertes, En bref (régularité, poids, séances, repas respectés 28 j, activité, bilan hebdo), Profil, Objectifs (statuts), Programme (cycle), Nutrition, Poids et mensurations, Bilan hebdomadaire (dernier reçu), Le reste (compléments, formation, historiques), lien vers chaque éditeur. « Ouvrir » un client mène à sa fiche. Notes privées, feedbacks et calls : phases 10 et 18 (base), à venir.
- `Store.lireTout(cles, { dates: true })` renvoie aussi `maj_le`. Aucune écriture nouvelle, aucune migration. Tests : 64 pages, 19 flux, 55 vérifications — 0 erreur.

## 2026-09-25 — Passation : Claude Code prend le relais sur `index.html`
- **Pour Grok Bot.** À partir de maintenant, la refonte de `index.html` (phases restantes 10, 13, 15, 16, 18-base, 20) est menée par **Claude Code**, connecté directement à ce dépôt. Le partage des rôles ne change pas : **toi, uniquement `donnees/`** (aliments, recettes, programmes, exercices, liens vidéo) ; **`index.html`, jamais**. Lucas garde le dernier mot sur tout ce qui part en production (GitHub et Supabase).
- La coordination continue **ici, par le dépôt** : Claude Code écrit ses notes de version dans ce fichier (`NOTESCLAUDE.md`), tu réponds dans `NOTES-GROK.md` comme avant. Rien ne change dans la table `briefs`.
- Tout ce que Claude Code doit savoir (état réel en ligne = **v37, commit `8dcbe7d`**, plan des 20 phases, règles « zéro perte de données », DA, architecture, Supabase, points ouverts) est dans **`HANDOFF-CLAUDE-CODE.md`** à la racine. Tu peux le lire pour connaître l'état de l'app ; il n'y a rien à faire dedans pour toi.
- Nouveau dossier **`tests-locaux/`** : banc de test Playwright de `index.html` avec Supabase simulé et données fictives (aucune clé, aucune donnée client). Il ne te concerne pas, ne pas le modifier.
- **Demande pour toi (données) :** 14 fiches de `donnees/aliments.json` portent encore `vegetarien` + `vegan` à tort — `graisse-de-dinde`, `huile-de-foie-de-morue`, les 3 `bouillon-de-viande-et-legumes-type-pot-au-feu-*`, `saucisse-de-volaille-type-knack-contenant-du-soja-preemballee`, `biscuit-ou-cracker-aperitif-souffle-gout-bacon`, `pizza-au-chorizo-ou-salami-preemballee`, `pizza-kebab-preemballee`, `sandwich-baguette-merguez-ketchup-moutarde`, `sandwich-grec-ou-kebab-baguette-crudites`, `sandwich-grec-ou-kebab-pita-crudites`, `sandwich-pain-de-mie-complet-bacon-crudites-preemballe`, `sauce-kebab-preemballee`. Le `Normaliser` de l'app les corrige à l'import (vérifié sur la v37), mais la source JSON doit être juste : retirer `vegan` et `vegetarien` sur les 14, garder `pescetarien` seulement pour l'huile de foie de morue. Un commit `donnees/` séparé, rien d'autre dedans.

## 2026-09-25 — Correctif de sécurité 37.1 (index.html) — Claude Code
- 14 endroits où une valeur écrite par un client (ses propres données, qu'il peut modifier par l'API) s'affichait sans échappement, dont 13 dans les écrans du coach (fiche, « Préparer le call », « Son suivi », « Ses courbes », « Ses repas », « Ses séances », « Son programme ») : valeurs échappées (`esc`, `fmt`, `Array.isArray`) ; `dateFr` n'insère plus jamais une valeur brute dans du HTML.
- Nouveau test d'attaque `tests-locaux/verif-xss.js` (données client piégées, écrans coach et client) : 15 exécutions de script détectées sur la v37, aucune sur la 37.1. Banc complet sans erreur (64 pages, 19 + 13 + 14 + 13 + 15 vérifications).
- Aucune donnée ni règle de la base modifiée. Pour Grok Bot : rien à faire.

## 2026-09-25 — Phases 10 et 18 (partie base) livrées (index.html v38) — Claude Code
- **Migration Supabase appliquée** (6 `ALTER POLICY` sur `donnees`, aucune ligne touchée, aucun DROP/DELETE) : le coach peut écrire `feedbacks` et `notes_coach` dans la fiche d'un client ; le client **lit** `feedbacks` mais ne peut ni l'écrire ni la modifier ni l'effacer ; `notes_coach` est **illisible** par le client (même par l'API). Backup vérifié ligne à ligne avant (hors dépôt), comptages et empreintes identiques avant/après sur toutes les tables, 33 tests RLS (client, coach, anonyme, accès croisé, transformation d'une ligne en `feedbacks`, changement de `user_id`) conformes, avant (répétition annulée) et après.
- **Nouvelles clés (chez le client, écrites par le coach seul)** : `feedbacks` = `{ liste: [{ semaine (lundi ISO), fin, date, texte, bilan? }] }`, une entrée par semaine de bilan (`bilan` = instant d'envoi du bilan auquel le feedback répond : `envoye_a`, sinon `envoye_le` pour les bilans plus anciens) ; `notes_coach` = `{ texte, maj, avant? }` (`avant` = la version trouvée à l'ouverture de la fiche, pour rattraper un effacement par erreur). Elles ne passent pas par `Store.ecrire` (objet `CleCoach` : écriture **conditionnelle** — la ligne n'est écrite que si elle n'a pas bougé depuis la lecture (`maj_le`), sinon rien n'est écrit ; « enregistré » seulement si la base renvoie la ligne ; envois de feedbacks l'un après l'autre) ; `Store.ecrire`, `Store.envoyer` et la restauration de sauvegarde les refusent (`Store.clesCoachSeul`).
- Phase 10 : éditeur de feedback sous chaque bilan hebdo reçu dans « Préparer le call » (+ un éditeur pour la semaine visée tant qu'elle n'a pas de bilan ; texte vidé = feedback retiré après confirmation ; éditeurs fermés si les feedbacks n'ont pas pu être lus) ; côté client, « Feedback de ton coach » dans Mon suivi (en tête s'il a moins de 7 jours) et carte « Ton feedback est disponible » sur l'Accueil ; l'alerte « Bilan hebdo reçu — à lire » s'éteint seulement quand un feedback a été écrit **sous ce bilan** (comparé à l'instant d'envoi `envoye_a`, nouveau champ des bilans ; un mot écrit avant son arrivée, ou avant qu'il soit renvoyé, ne compte pas : « feedback à relire ») ; entrées `I18N.en` ajoutées.
- Phase 18 : « Notes privées » dans la fiche client, enregistrées toutes seules (1 s après la dernière frappe, et tout de suite en quittant la fiche) ; si la lecture échoue, le champ reste fermé ; écriture conditionnelle : si les notes ont été modifiées ailleurs (autre onglet ou appareil), rien n'est écrasé et les deux versions sont gardées (celle du coach à la suite, datée) ; une écriture dont la réponse s'est perdue est reconnue et reprise ; la version trouvée à l'ouverture est gardée (`avant`). « accueil » ajouté à `MODIFIABLES`. Les écritures du coach (`feedbacks`, `notes_coach`) ne comptent plus comme activité du client. « Calls » : laissé de côté (décision de Lucas).
- Tests : banc local complet — 64 pages sans erreur ni écriture, 19 flux, 13 + 14 + 13 + 15 vérifications v34–v37, **66 vérifications v38** (`tests-locaux/verif38.js`, Supabase simulé avec les règles d'accès réelles et les écritures conditionnelles : conflits entre onglets, réponse perdue, bilan renvoyé, session perdue, refus silencieux, refus 403, lecture en panne, restauration, anglais, mobile) et le test d'attaque `verif-xss.js` (5/5). Le banc tourne aussi avec le Chrome du système (`tests-locaux/chrome-systeme.js`).
- **Pour Grok Bot** : rien à faire côté `donnees/`. Ne jamais écrire les clés `feedbacks` ni `notes_coach`. La demande du 25/09 sur les 14 fiches d'`aliments.json` reste valable. Prochaine phase : 15 (FREE / CLIENT, colonne `profils.statut`).

## 2026-09-25 — Phase 15 (FREE / CLIENT) livrée (index.html v39) — Claude Code
- **Migration Supabase appliquée** (table `profils`, aucune ligne supprimée) : colonne `statut` ajoutée sans défaut, les 9 comptes existants passés à `'client'`, puis `NOT NULL` et défaut `'prospect'` pour les comptes créés ensuite, contrainte `statut in ('prospect','client')`, trigger `protege_role` étendu : `role` et `statut` ne changent que par un coach (ou la clé de service des fonctions serveur) ; un compte ne peut pas se passer « client » lui-même. Backup vérifié (structure relevée juste avant), comptages identiques avant/après, 19 tests SQL (répétition annulée puis état réel).
- Au passage : la création d'un accès « complet » par la fonction `creer-acces` échouait en silence (le trigger refusait la clé de service) — corrigé par le même trigger.
- App : `Auth.estProspect()`, pastille « prospect » dans Mes clients, statut + bouton « Passer client / Repasser prospect » dans Comptes, statut « client » posé tout de suite après la création d'un accès (fenêtre d'alerte si ça échoue), pas d'alerte de suivi pour un prospect, KPI « Prospects ». Inscription libre codée (`Auth.inscrire`, écran « Créer mon compte ») mais **fermée** : `CONFIG.marque.inscription_libre = false` ; Lucas l'ouvrira lui-même à la fin (ce réglage + « Allow new users to sign up » dans Supabase). Bug corrigé : « Supprimer » sur un compte d'équipe déclenchait aussi « Ouvrir sa fiche ».
- v38 renforcée : un feedback retient toutes ses tentatives sans réponse (plus de faux conflit après deux échecs de suite).
- Tests : 64 pages sans erreur ; 19 + 13 + 14 + 13 + 15 + 67 vérifications, verif-xss 5/5, **verif39 35/35**. Les simulations routent désormais par nom d'hôte (une requête d'inscription de test était partie vers la vraie base : refusée par Supabase, aucune donnée créée).
- **Pour Grok Bot** : rien à faire côté `donnees/`. Prochaine phase : 16 (écrans verrouillés pour les prospects + Calendly).

## 2026-09-25 — Phase 16 (écrans verrouillés + Calendly) livrée (index.html v40) — Claude Code
- Mode gratuit pour les prospects : seuls **Accueil, Speed Formation et Profil** sont ouverts (liste blanche `CONFIG.marque.gratuit_ouverts` : tout onglet ajouté plus tard est verrouillé par défaut). Les autres onglets portent un petit cadenas et affichent « Cette fonctionnalité est disponible avec l'accompagnement MHX. », ce qu'ils apportent, et « Réserver mon appel » (Calendly, `CONFIG.marque.calendly`, nouvel onglet). Une page verrouillée ne lit aucune donnée.
- Accueil du prospect : bienvenue, son espace gratuit (formation, profil), ce que débloque l'accompagnement, appel découverte. **Aucun prix** dans l'application.
- Sur téléphone, la barre du bas d'un prospect montre d'abord ses onglets ouverts (Accueil, Formation, Profil) ; les onglets verrouillés sont annoncés « (verrouillé) » aux lecteurs d'écran ; après son premier questionnaire, il revient à l'accueil. Le statut est lu à l'ouverture de l'application : un « Passer client » prend effet à la prochaine ouverture.
- Rien ne change pour les clients, ni pour le coach qui consulte une fiche (même celle d'un prospect), ni tant que la base n'a pas de colonne `statut`. Toutes les chaînes ont leur entrée `I18N.en`.
- Aucune migration. Tests : 64 pages sans erreur ; 19 + 13 + 14 + 13 + 15 + 67 vérifications, verif-xss 5/5, verif39 35/35, **verif40 26/26**.
- **Pour Grok Bot** : rien à faire. Prochaine phase : 13 (photos de progression, Storage).

## 2026-09-25 — Phase 13 (photos de progression) livrée (index.html v41) — Claude Code
- **Supabase Storage** : bucket **privé** `photos` (5 Mo max, JPEG seulement), créé **après** ses quatre règles d'accès sur `storage.objects` : chacun dépose, remplace, lit et retire dans son dossier `<user_id>/` seulement, avec un nom imposé `sNNN-face|profil|dos.jpg` ; le coach lit tout et n'écrit rien ; l'anonyme ne voit rien. Aucune donnée existante touchée (0 bucket et 0 objet avant). 18 tests SQL (répétition annulée puis état réel) ; comptages et empreintes identiques avant/après sur toutes les tables.
- **App** (objet `Photos`, section « Photos de progression » dans Ma progression) : trois vues (face, profil, dos) par semaine ; l'image est réduite dans le navigateur (1 080 px au plus, JPEG) avant l'envoi ; comparaison de deux semaines côte à côte ; retrait avec confirmation. Nouvelle clé **client** `photos` = `{ liste: [{ semaine, date, vues: { face, profil, dos } }] }` (chemins des fichiers). Le coach les voit en lecture seule dans « Ses courbes ». Un prospect n'y a pas accès (Ma progression est verrouillée en mode gratuit). Toutes les chaînes ont leur entrée `I18N.en`.
- **Suppression de compte** (`supprimer-acces`) : efface d'abord les photos du compte et vérifie qu'il n'en reste aucune ; si le stockage ne répond pas, rien n'est effacé (message clair) ; l'identifiant de compte est vérifié.
- Robustesse : une photo déjà disparue se retire quand même ; si l'index des photos ne peut pas être lu, l'envoi est désactivé (sinon une photo en remplacerait une autre sans que l'index suive) ; le coach choisit les semaines à comparer ; une photo affichée deux fois n'est téléchargée qu'une fois.
- Tests : 64 pages sans erreur ; 19 + 13 + 14 + 13 + 15 + 67 vérifications, verif-xss 5/5, verif39 35/35, verif40 26/26, **verif41 25/25** ; banc de la fonction 17/17.
- **Pour Grok Bot** : rien à faire côté `donnees/`. Ne jamais écrire la clé `photos`. Prochaine et dernière phase : 20 (tests complets).

## 2026-09-25 — Phase 20 (tests complets et robustesse) livrée (index.html v42) — Claude Code — les 20 phases sont faites
- **Robustesse** : un client peut écrire n'importe quoi dans ses propres clés par l'API. À la lecture, l'app remet désormais la structure d'aplomb (objet `Forme` : un conteneur du mauvais type devient vide, une liste d'objets perd ses éléments invalides ; les valeurs ne sont jamais modifiées — vérifié sans effet sur toutes les données réelles). Un client aux données illisibles n'emporte plus le tableau de bord du coach : sa ligne reste, avec l'alerte « Données illisibles — à vérifier ». Une page qui ne peut pas s'afficher le dit clairement au lieu de rester blanche. Une donnée lue pour un client n'est jamais enregistrée chez un autre, même si le coach change de fiche pendant un chargement ou une opération (défauts trouvés en relecture et corrigés : historique, cache, envoi annulé en quittant une fiche) ; une écriture refusée est annoncée au lieu d'être perdue en silence.
- **Sécurité** : échappement de l'apostrophe ; seuls des identifiants YouTube valides construisent un lecteur vidéo ; **migration Supabase** : déposer ou remplacer une photo est réservé aux comptes client (fonction `est_client()`), pour qu'un futur prospect ne puisse pas remplir le stockage. Aucune donnée touchée ; 14 tests SQL (répétition annulée puis état réel) ; comptages identiques avant/après.
- **Tests complets** : banc complet en français et en anglais, thèmes sombre et clair, mobile et desktop (64 pages chacun, 0 erreur, 0 écriture) ; toutes les suites v34 → v42 ; `verif42.js` (20 : trois clients aux données piégées, 54 pages parcourues sans erreur) ; point RLS final : tous les tests SQL des phases 10 à 20 rejoués sur la base réelle (transactions annulées).
- **Pour Lucas** : la liste de ses vérifications à l'écran est au §11 de `HANDOFF-CLAUDE-CODE.md`. L'inscription publique reste fermée : il l'ouvrira lui-même (prérequis au §7).
- **Pour Grok Bot** : rien à faire côté `donnees/`. La demande du 25/09 sur les 14 fiches d'`aliments.json` reste valable.

## 2026-09-25 — Correctif d'affichage côté coach (index.html v43) — Claude Code
- Deux défauts relevés par Lucas en testant la v42. Aucune migration, aucune requête ajoutée, aucune donnée touchée ; écrans coach seulement (rien ne change pour les clients).
- **Compte sans prénom ni nom** : il s'affichait avec le début de son identifiant (« 9df6bb84 », initiale « 9 ») dans le tableau de bord, Mes clients et sa fiche. Il s'affiche maintenant « Sans nom » partout (tableau de bord, Mes clients, Comptes, fiche, bandeau, fil d'Ariane, fenêtres de confirmation), avec un avatar neutre « ? » en gris. Un seul endroit décide du nom affiché : `Clients.nom(p)` (+ `Clients.avatar(nom)`, `Clients.SANS_NOM`).
- **Compte qui n'a jamais rien saisi** : il était compté dans « Sans nouvelles depuis 10 jours ou plus » (Mes clients). Il a maintenant son propre encadré « Jamais rien saisi : … Ce compte n'a encore rien enregistré : vérifie que ses accès sont bien arrivés. » ; « Sans nouvelles » ne cite plus que les comptes réellement inactifs depuis 10 jours ou plus. Les alertes de la fiche et du tableau de bord (« Jamais rien saisi » / « Inactif depuis N j ») distinguaient déjà les deux cas : inchangées.
- Tests : nouveau `tests-locaux/verif43.js` (34 vérifications : deux comptes ajoutés au banc — un sans nom ni donnée, un nommé sans donnée — tableau de bord, Mes clients, Comptes, fenêtres annulées, fiche, fil d'Ariane, témoin sans compte muet ; aucune erreur, aucune écriture ; sur la v42 il tombe à 16/34) ; banc complet rejoué : 64 pages en français, en anglais et en thème clair sans erreur ni écriture, 19 + 13 + 14 + 13 + 15 + 67 vérifications, verif39 35/35, verif40 26/26, verif41 25/25, verif42 20/20, verif-xss 5/5. Relecture par un sous-agent indépendant avant le push.
- **Pour Grok Bot** : rien à faire côté `donnees/`. La demande du 25/09 sur les 14 fiches d'`aliments.json` reste valable.

## 2026-09-25 — Funnel « Challenge 7 jours » : v44, le socle (index.html v44) — Claude Code
- **Nouveau chantier** (plan complet dans `PLAN-FUNNEL-7-JOURS.md`, validé par Lucas le 25/09) : transformer l'app en funnel Instagram → inscription gratuite → Challenge 7 jours → personnalisation → appel Calendly. Quatre versions : v44 socle, v45 jours 2 à 4, v46 jours 5 à 7, v47 finitions (retour du lien d'email, côté coach, marche à suivre). **Aucune migration Supabase** : la progression est une nouvelle clé client `challenge` dans `donnees` (règles d'accès propriétaire existantes ; le coach la lit, ne l'écrit jamais) et les réponses du jour 1 vont dans la clé `intake` (mêmes identifiants que le questionnaire). L'inscription publique reste **fermée** (`inscription_libre: false`) : aucun prospect n'existe encore, **rien ne change pour les 7 clients ni pour le coach**.
- **Inscription** (écran « Rejoins le Challenge 7 jours », visible seulement quand `inscription_libre` vaut true) : prénom, email, mot de passe (8 caractères, bouton « Afficher »), case obligatoire « J'accepte les conditions d'utilisation et la politique de confidentialité » (texte intégré, volet), note « ne remplace pas un avis médical ». Le nom n'est plus demandé (il viendra avec le questionnaire) ; la date d'acceptation et la version des conditions partent dans les métadonnées du compte (`raw_user_meta_data`, sans migration ; le déclencheur `creer_profil` ne lit que prénom et nom).
- **Outil « Challenge 7 jours »** (`#/challenge`, `#/challenge/N`, prospects seulement, drapeau `prospect_seul`) : 7 étapes, barre de progression, **jour 1 « Ton point de départ »** = une douzaine de questions du questionnaire (rendu `outilProfil.champ`), puis le point de départ en tuiles (écart avec le poids objectif, dépense estimée par les formules du calculateur — intouchées, `outilCalculateur.departDepuis` —, séances, sommeil, énergie) avec une lecture prudente (« estimation, pas une vérité »), et « J'ai fait mon point de départ ». Jours 2 à 7 : titres en place, page « Ce jour arrive bientôt » (v45 / v46). Déblocage : un jour par jour calendaire (`CONFIG.challenge.un_jour_par_jour`), rattrapage dans l'ordre si on est en retard, un jour manqué ne remet rien à zéro. Textes dans `CHALLENGE7` (français) et `CHALLENGE7.en` (anglais, même forme, versés au dictionnaire par `Contenus.zip` au démarrage).
- **Accueil du prospect** = hub du challenge (progression, jour du moment, « Mon compte ») ; l'accompagnement (Calendly) n'y apparaît qu'à partir du jour 5 ou à la fin (CTA progressifs) ; la **Speed Formation est verrouillée** pour un prospect jusqu'à la fin des 7 jours (`CONFIG.challenge.formation_apres_jour7`, décision de Lucas). Démarrage : un prospect arrive sur le jour 1, jamais sur le questionnaire complet ; son Profil ne montre que « Mon compte » et « Mes données » ; un ancien prospect passé client retrouve le questionnaire complet pré-rempli.
- **Mode test pour Lucas** : `#/challenge-libre` débloque tous les jours sur l'appareil (pastille « Mode test » sur l'accueil et le challenge), `#/challenge-rythme` remet le rythme normal ; rien n'est écrit en base (drapeau local, effacé à la déconnexion).
- Clé `challenge` = `{ version: 1, debut (date locale du jour 1), jours { "1": { fait (instant), date (date locale), … } }, cta { clics[{jour, date}] }, termine (instant) }` ; `Forme.modeles.challenge` ; export / restauration « Mes données » la connaissent. Autres ajouts : `CONFIG.challenge`, `CONFIG.marque.gratuit_ouverts` = accueil, challenge, profil ; sous-route `#/…/N` (`sousRoute`) ; `Auth.inscrire(…, extra)` ; icône `challenge` ; entrées `I18N.en`.
- Tests : nouveau `tests-locaux/verif44.js` (81 vérifications : inscription, démarrage, jour 1 complet avec ses écritures, déblocage / rattrapage / ordre, mode test, challenge terminé (formation, Calendly, clic noté), clé piégée, anglais, mobile, client et coach inchangés ; sur la v43 il tombe à 0/4 puis interruption) ; `verif39` et `verif40` mis à jour (inscription simplifiée, hub, formation verrouillée, profil allégé) ; `rig.js` : persona prospecte « Léa » (6 pages) et écran d'inscription (78 pages en français, en anglais et en thème clair, 0 erreur, 0 écriture) ; suites v34 → v43 et verif-xss inchangées et vertes. Sauvegarde de la base vérifiée avant (28/28 lignes, empreintes, hors dépôt) bien qu'aucune migration : empreintes identiques après. Relecture par un sous-agent indépendant avant le push : 15 réserves (aucune bloquante), toutes corrigées — bornes de saisie et garde-fou santé au jour 1 (âge 18 à 90, taille 120 à 230 cm, poids 35 à 250 kg ; poids ou objectif sous un poids de forme habituel → pas d'écart affiché, renvoi vers un professionnel de santé), touche Entrée de l'inscription, dictionnaire anglais chargé même quand la langue vient de la base, un jour ne reste jamais bloqué à vie, `termine` = 7 jours faits, page verrouillée de la Speed Formation (« s'ouvre à la fin de ton challenge »), étapes accessibles. À arbitrer par Lucas : l'âge minimum (18, `CONFIG.challenge.bornes`) et Calendly sur les pages verrouillées dès le jour 1.
- **Pour Grok Bot** : rien à faire côté `donnees/`. Ne jamais écrire la clé `challenge`. La demande du 25/09 sur les 14 fiches d'`aliments.json` reste valable. Prochaine version : v45 (jours 2, 3, 4 : nutrition, mini-séance avec les vidéos du catalogue d'exercices, habitudes).

## 2026-09-25 — Funnel « Challenge 7 jours » : v45, jours 2, 3 et 4 (index.html v45) — Claude Code
- **Jour 2, nutrition** : cinq erreurs fréquentes à choisir (sauter des repas, boire ses calories, trop peu de protéines, le tout ou rien, manger sans repère), l'action du jour dépend du choix, ressenti Facile / Moyen / Dur, « Action faite » → `jours["2"] = { erreur, ressenti, fait, date }`.
- **Jour 3, entraînement** : échauffement (le « Cardio léger » embarqué, `ECHAUFFEMENTS`), circuit de 5 exercices au poids du corps (squats, pompes, fentes arrière, pont fessier, gainage) avec le nombre de tours selon le niveau du questionnaire (débutant 2, intermédiaire 3, avancé 4), bouton « Démonstration » par exercice (vidéo du catalogue, lecteur youtube-nocookie chargé au clic seulement), consigne de sécurité, tours réalisés + ressenti 1 → 5 → `jours["3"] = { tours, ressenti, fait, date }`.
- **Jour 4, habitudes** : cinq habitudes à choisir, intention « quand / où », texte mindset, carte « Ce que change un coach à ce stade » (sans bouton : les CTA commencent au jour 5), « Habitude faite aujourd'hui » → `jours["4"] = { habitude, quand, ou, fait, date }`.
- Un jour fait se relit en récapitulatif (choix, action, ressenti). Textes FR dans `CHALLENGE7.jours[1..3]`, EN dans `CHALLENGE7.en.jours[1..3]` (même forme, contrôle statique). **Relecture de la clé `challenge` juste avant chaque écriture** (`Challenge.fraiche` : connexion renouvelée si besoin, écriture locale en attente envoyée d'abord, fusion des jours et clics déjà présents) : un onglet resté ouvert ailleurs n'écrase plus un jour validé entre-temps, un jour déjà validé ailleurs n'est jamais réécrit (la page se relit) ; si la relecture échoue, rien n'est écrit, la copie en mémoire garde l'état affiché et la page ne passe pas en « validé ». Les jours 5 à 7 restent « bientôt » (v46).
- Aucune migration, aucune requête nouvelle ; rien ne change pour les clients ni pour le coach. Inscription toujours fermée.
- Tests : nouveau `tests-locaux/verif45.js` (54/54 : jours 2, 3, 4 complets avec leurs écritures, choix obligatoires, action qui suit le choix, tours selon le niveau, lecteur vidéo, relecture avant écriture, lecture seule des jours faits, données piégées, relecture en panne, anglais, mobile, client et coach ; sur la v44 : 0/2 puis interruption) ; `verif44` 81/81 sur la v45 ; banc complet (v34 → v45, verif-xss, flux, 78 pages FR / EN / clair) vert. Relecture par un sous-agent indépendant avant le push : 13 réserves puis 6 au second tour (aucune bloquante), toutes corrigées — jeton renouvelé avant la relecture, marqueur d'écriture en attente nettoyé dans `Store.ecrire` (seule la valeur réellement en attente repart), message « déjà validé sur un autre appareil », jour déjà validé ailleurs, copie en mémoire après une relecture ratée, félicitations des jours 2 à 4 (et « dix fois mieux » retiré : aucun chiffre inventé), test qui laissait partir une requête, quand / où obligatoires au jour 4, récapitulatif borné, accessibilité (groupes nommés, message annoncé, focus, une vidéo à la fois), textes des durées. Empreintes de la base identiques avant / après.
- **Pour Grok Bot** : rien à faire côté `donnees/`. Les vidéos du jour 3 sont celles du catalogue `exercices` (squats G9nGRJjQFXw, pompes jdM0CTRKTKU, fentes arrière hYbhf31d49w, pont fessier bnKpYIMmvtw, gainage R4FB7v-v6x0) : si un lien change dans `exercices.json`, le dire ici pour que `CHALLENGE7` suive. Prochaine version : v46 (jours 5, 6, 7 + CTA progressifs).

## 2026-09-26 — Funnel « Challenge 7 jours » : v46, jours 5, 6 et 7 + appels à l'action (index.html v46) — Claude Code
- **Jour 5, personnalisation** : une page calculée à partir des réponses des jours 1 à 4 (formules du calculateur, intouchées, via `outilChallenge.perso`) : objectif et écart, rythme prudent (`CONFIG.challenge.rythme`), dépense estimée, cible calorique (−10 % / +10 % de `CONFIG.calcul`) et protéines, entraînement (séances → corps entier ou haut / bas, rappel de la séance du jour 3, niveau et matériel), alimentation (l'erreur du jour 2 = premier levier, repas par jour), habitudes (jour 4, sommeil, énergie). **Garde-fou santé** : poids ou objectif sous un poids de forme → aucune cible en déficit, renvoi vers un professionnel. Lien discret « Voir ce que comprend l'accompagnement » → volet (`AVANTAGES`) avec un appel Calendly en bouton fantôme. « Ce qui te parle le plus » (facultatif) + « J'ai lu mes recommandations » → `jours["5"] = { interet }`.
- **Jour 6, projection** : trois colonnes (aujourd'hui, ton objectif avec le « pourquoi » du jour 1, 90 jours en trois phases), fourchette prudente sur 12 semaines **plafonnée à l'écart** (perte : 0,25 à 0,5 kg / semaine ; prise : 0,1 à 0,25) avec « Personne ne peut te garantir un chiffre », rien en kilos pour recomposition / performance / santé ; « Où en es-tu ? » (avancer / j'hésite / pas maintenant, décision de Lucas) → `jours["6"] = { etat }` ; appel Calendly secondaire.
- **Jour 7, conversion** : la page se **valide à l'ouverture** (c'est la fin du challenge : `termine`, la Speed Formation s'ouvre dans la foulée), tuiles (jours, écart, cible, habitude, séance), « Maintenant, construisons ton plan personnalisé. », appel principal **RÉSERVER MON APPEL** (Calendly existant), case « J'ai réservé mon appel » → `jours["7"].reserve` (daté), « Pas encore le moment ? », Instagram, lien vers la formation.
- **CTA progressifs** : rien avant le jour 4 (carte d'information), jour 5 volet, jour 6 bouton secondaire, jour 7 bouton principal, puis le hub (« Avec l'accompagnement MHX » à partir du jour 5). **Chaque clic Calendly est noté** dans `challenge.cta.clics` (jour + instant) pour le coach (bloc de la fiche en v47). `Challenge.completer` (compléter un jour fait sans le revalider).
- **Après la relecture indépendante (réserves corrigées avant le push)** : file d'écriture `Challenge.enFile` (valider / completer / clic ne se chevauchent jamais : un clic Calendly et une validation lancés ensemble se suivent, aucun n'efface l'autre) ; le jour 7 n'annonce la fin que si elle est bien enregistrée (sinon encart « Ta dernière étape n'a pas pu être enregistrée », sans case « J'ai réservé ») ; lecture ratée de la clé ou du questionnaire → encart « Recharge la page » au lieu d'une page trompeuse ; `Challenge.completer` renvoie null si le jour n'est pas fait ; fourchette du jour 6 : « à la portée d'un rythme durable » quand l'écart est sous la borne basse (plus de « 2,0 à 2,0 »), rien si le poids objectif va dans l'autre sens que l'objectif ou lui est égal ; rythme affiché « 0,25 » (plus « 0,3 ») ; « Ton premier levier : corriger « … » » ; pseudo Instagram une seule fois ; titre court « Ta dernière étape » dans le hub ; bouton Calendly du hub en fantôme tant que le challenge n'est pas terminé ; focus (volet, après « J'ai réservé ») ; « pourquoi » coupé proprement. Deux arbitrages restent pour Lucas : protéines à 2,2 g/kg pour les poids élevés, intensité du bouton Calendly du hub dès le jour 5. Second tour de relecture : VALIDE, trois remarques mineures corrigées dans la foulée (fourchette calculée au dixième de kilo, file d'écriture bornée à 15 s, habitude citée entre guillemets).
- Textes FR / EN de même forme (contrôle statique) ; aucune migration, aucune requête nouvelle ; rien ne change pour les clients ni pour le coach ; inscription toujours fermée. Le challenge est complet : les 7 jours sont livrés.
- Tests : nouveau `tests-locaux/verif46.js` (74/74 : jour 5 avec les chiffres exacts du calculateur, garde-fou santé, recomposition, volet et clic noté, hub après le jour 5 ; jour 6 avec fourchette plafonnée, prise, santé, clic, état obligatoire ; jour 7 validé à l'ouverture, tuiles, appel, case réservé, formation ouverte, jour 7 inaccessible sans le 6, revu sans réécriture ; relecture ratée à l'ouverture ou juste avant la validation du jour 7, écritures presque simultanées, garde-fou santé aux jours 6 et 7, fourchettes (à la portée, autre sens, écart nul), questionnaire illisible, anglais (jours 5, 6, 7), mobile 390 et 320 px, thème clair, mode test, client et coach ; sur la v45 : 0/9 puis interruption) ; `verif44` 81/81 et `verif45` 54/54 rejoués ; banc complet (v34 → v46, verif-xss, flux, 78 pages FR / EN / clair) vert. Relecture par un sous-agent indépendant avant le push (deux tours). Base vérifiée ligne par ligne avant le push : 28 lignes, aucune ajoutée ni supprimée, aucun identifiant de test ; seules deux progressions `formation` ont changé, modifiées par leurs propres clients le 25/09 au soir (usage normal, rien à voir avec le banc).
- **Pour Grok Bot** : rien à faire côté `donnees/`. Prochaine version : v47 (retour du lien de confirmation d'email, écran « Vérifie ta boîte mail », pastille et bloc « Challenge » côté coach, marche à suivre pour l'ouverture avec SMTP externe).

## 2026-09-26 — Funnel « Challenge 7 jours » : v47, finitions et marche à suivre d'ouverture (index.html v47) — Claude Code
- **Retour du lien de confirmation d'email** : `demarrer()` reconnaît `access_token` + `refresh_token` + `type=signup` (ou `magiclink`) dans l'adresse → `entrerParLien()` lit la personne avec ce jeton (`GET /auth/v1/user`), mémorise la session comme après une connexion (« Rester connecté »), retire les jetons de l'adresse, et le démarrage continue : un prospect arrive directement sur son jour 1 avec « Ton email est confirmé. Bienvenue dans le challenge ! ». Lien périmé ou déjà utilisé (Supabase revient **sans jeton**, avec `#error=…&error_code=otp_expired…`) → adresse nettoyée d'abord (sinon boucle), « Ce lien n'est plus valable (il ne sert qu'une fois)… » en français, jamais le texte anglais brut, et une session déjà ouverte n'est pas touchée (mot « tu es déjà dans ton espace »). Un lien valide alors qu'un **autre compte** est connecté sur l'appareil ne bascule jamais en silence (mot « Un autre compte est déjà connecté… », le lien n'est pas utilisé). Panne réseau sur la lecture de la personne → « Ton email est bien confirmé, mais l'entrée automatique a échoué ». Messages Supabase du parcours traduits (« Email not confirmed », limites d'envoi). `type=recovery` inchangé ; les autres jetons (changement d'email) sont retirés de l'adresse.
- **Écran « Vérifie ta boîte mail »** : après une inscription quand Supabase exige la confirmation (réponse sans session), la carte du portail devient un écran dédié (adresse, indésirables, « Retour à la connexion ») ; `ecranVerifieEmail()`. Textes FR / EN dans `CHALLENGE7.inscription`.
- **Côté coach, lecture seule, aucune requête nouvelle** (la clé `challenge` est déjà dans ce que `Clients.charger` télécharge) : `Challenge.resume`, `Challenge.pastilleCoach` ; Mes clients → « Challenge n/7 », « Challenge terminé », « a cliqué Réserver », « appel réservé » sur les prospects ; tableau de bord → tuile Prospects « n challenge(s) terminé(s) » ; fiche → bloc « Challenge 7 jours » (`outilAccueil.challengeFicheHTML` : 7 étapes, jours validés et dates, erreur du jour 2 + action jugée, séance du jour 3, habitude du jour 4 avec quand / où, intérêt du jour 5, réponse du jour 6, clics « Réserver », case « J'ai réservé ») + phrase du haut. Rien pour un client sans challenge ; un ancien prospect passé client garde son bloc dans la fiche (historique utile), sans pastille dans Mes clients. Données piégées par un prospect (objets à la place des chaînes, HTML dans le texte libre) : rien ne tombe, rien ne s'exécute (`resume` sans `etat()`, chaînes seules, `pastilleCoach` et `challengeFicheHTML` sous try/catch). `Clients.charger` lit désormais **par pages de 1 000** (Supabase coupait à 1 000 lignes sans prévenir : avec l'inscription ouverte, quelques centaines de prospects auraient fait disparaître des clients de la liste).
- **`OUVERTURE-INSCRIPTION.md`** (racine) : la marche à suivre de Lucas, dans l'ordre : SMTP externe gratuit (Brevo) → Supabase Auth (« Allow new users to sign up » + « Confirm email ») → adresses de retour → modèle d'email en français → `inscription_libre: true` en dernier → test avec une adresse jetable → interdits (CAPTCHA seul) → lien Instagram → quoi regarder ensuite → refermer.
- Aucune migration ; inscription toujours fermée ; rien ne change pour les clients ni pour le prospect. **Le chantier funnel (v44 → v47) est livré** ; il reste l'ouverture par Lucas.
- Tests : nouveau `tests-locaux/verif47.js` (46/46 : lien valide → session, adresse nettoyée, jour 1, mot d'accueil, rechargement ; jeton refusé ; lien périmé réel `#error=otp_expired` sans session (la connexion qui suit aboutit, pas de boucle) et avec le coach connecté ; lien valide avec un autre compte connecté (pas de bascule) ; panne serveur ; lien magique d'un client (mot neutre) ; adresse mal encodée ; lien de réinitialisation intact ; anglais ; touche Entrée sur « Vérifie ta boîte mail » ; « email rate limit exceeded » et « Email not confirmed » en français ; prospect piégé côté coach (Mes clients entière, tableau de bord, fiche, rien d'injecté) ; ancien prospect passé client ; plus de 1 000 profils et lignes (Mes clients lu par pages, complète, sans doublon) ; « Vérifie ta boîte mail » FR et EN mobile, retour à la connexion, entrée directe sans confirmation ; Mes clients 3/7, 6/7 + clic, terminé + réservé, 0/7 ; tableau de bord ; fiche 3 jours, 6 jours, terminée, sans challenge, client Thomas ; mobile ; client et prospecte inchangés ; sur la v46 : 10/47 (dont une erreur JS sur une adresse mal encodée, corrigée en v47)) ; `verif46` 74/74, `verif45` 54/54, `verif44` 81/81 rejoués ; banc complet (v34 → v47, verif-xss, flux, 78 pages FR / EN / clair) vert. Relecture par un sous-agent indépendant avant le push : 10 réserves dont 2 bloquantes **pour l'ouverture** (lien périmé réel, prospect piégé côté coach), toutes corrigées et testées avant le push ; second tour VALIDE, six points mineurs corrigés dans la foulée (mot si la session enregistrée est périmée après un lien, forme du jeton vérifiée avant l'appel, 429 = trop de demandes, pagination avec 416, doublons et page répétée, `tours` piégé, clics du test sans interruption), deux points négligeables laissés tels quels (« Rester connecté » décoché à l'inscription est ignoré au retour par lien : la session est enregistrée ; un lien valide ouvert sur un appareil où un autre compte a une session morte affiche « Ce lien n'est plus valable » alors qu'il a servi, le conseil de se connecter reste juste). Base vérifiée avant le push : 29 lignes (une ligne `prefs` ajoutée par un client le 26/09 vers 4 h 30, usage normal), aucun identifiant de test, profils inchangés.
- **Pour Grok Bot** : rien à faire côté `donnees/`. Prochaine étape : l'ouverture par Lucas (`OUVERTURE-INSCRIPTION.md`) ; un commit dédié `inscription_libre: true` quand il le décide.

## 2026-09-26 — Chantier P0 (brief de Lucas après son travail avec Grok) : v48, fiabilité et sécurité (index.html v48) — Claude Code
- **État des lieux fait avant de coder** (code + Supabase) : RLS en place sur toutes les tables ; un prospect ne peut pas se passer client (déclencheur `protege_role` vérifié) ; photos réservées aux clients ; 8 clients + 1 coach, emails tous confirmés ; conseiller Supabase : deux alertes bénignes (`est_coach` / `est_client` appelables : elles ne disent à chacun que son propre rôle) et « mots de passe compromis » désactivé (réglage du tableau de bord, offre Pro).
- **Table `briefs` fermée** (migration non destructive, `ALTER POLICY`) : les 14 messages internes Claude / Grok étaient lisibles avec la clé publique (anon), dont un avec le nom complet d'une cliente et des éléments de son programme, deux avec des adresses email. Désormais : coach connecté seulement. Retour arrière (une ligne) dans `sauvegardes/2026-09-26-avant-v48/regle-briefs-avant.sql`. **Pour Grok Bot** : les briefs ne se lisent plus sans connexion ; le canal est `NOTESCLAUDE.md`.
- **Sessions** (`Auth`) : plusieurs onglets (ou l'app installée + le navigateur) ne se déconnectent plus mutuellement — la session renouvelée par un onglet est reprise par les autres (`plusRecente`, événement `storage`, attente de 1,5 s si un autre onglet renouvelle au même moment) ; un renouvellement impossible faute de réseau ne déconnecte plus (la session est gardée) ; un 401 en cours de route déclenche un renouvellement puis UN nouvel essai ; un seul renouvellement à la fois par onglet.
- **Saisies gardées sur l'appareil** (`Store`) : chaque modification est copiée (une clé par copie, `mhx_attente|compte|clé`, au même endroit que la session : « Rester connecté » décoché = la copie disparaît avec l'onglet) jusqu'à son arrivée au serveur ; elle repart d'elle-même (retour du réseau, retour sur l'onglet, toutes les 30 s, démarrage suivant), seulement si le serveur n'a rien de plus récent (sinon c'est dit : « Une modification faite hors ligne n'a pas été envoyée… »), seulement pour le compte qui l'a saisie, et jamais après une saisie plus récente du même onglet (`dernierT`, copie relue juste avant l'envoi) ; départ immédiat à la fermeture de l'onglet (`pagehide`, keepalive) ; `maj_le` = instant de la saisie ; refus définitif (403…) → copie retirée et « Non enregistré — modification refusée » ; « gardé sur cet appareil » n'est affiché que si la copie existe vraiment. **Session refusée en cours d'usage** (déconnexion depuis un autre appareil) : un bandeau « Ta session a pris fin — Me reconnecter » ; la page reste intacte (un feedback ou une note en cours se copie) ; la saisie gardée repart après reconnexion. Une copie déjà arrivée (même instant que le serveur) est retirée sans fausse alerte ; une horloge d'appareil qui recule ne fait rien perdre (la garde « jamais plus ancien » ne vaut que pour les copies reprises). **Déconnexion** : ce qui attend part d'abord (avec le jeton) ; s'il reste des copies non envoyées, confirmation avant de tout effacer.
- **Questionnaire** : les champs texte partent pendant la frappe (avant : seulement en quittant le champ). **Diagnostic du jour 1** : chaque réponse valide part en brouillon pendant la saisie, **mais rien ne part tant que l'âge n'est pas rempli et d'au moins 18 ans** (les réponses d'une personne mineure n'arrivent jamais en base) ; une valeur hors bornes, jamais ; la personne retrouve ses réponses en revenant ; « Voir mon point de départ » vérifie une copie avant de toucher au questionnaire. `verif44` : l'étape « 15 ans » garde « aucune écriture » ; l'étape « Voir » regarde le dernier envoi (des brouillons valides ont pu partir avant).
- **Protection des contenus premium** : le coaching personnalisé (programme, diète, calories, compléments, retours) est protégé par la base. Tout ce qui est dans `index.html` ou le dépôt public (Speed Formation, catalogue) est public par construction : ne jamais y mettre de contenu payant.
- Tests : nouveau `tests-locaux/verif48.js` (40/40 ; sur la v47 : 2/7 puis arrêt), `verif44` 81/81 (étape « Voir » adaptée), banc complet FR / EN / clair ; relecture indépendante en trois tours (réserves corrigées : âge, course reprise / saisie, libellés exacts, session refusée → bandeau sans détruire la page, copie écartée signalée sans fausse alerte, déconnexion, une clé par copie, horloge qui recule) ; base vérifiée avant et après la migration.
- **À consigner par Lucas (RGPD, art. 33.5)** : la table `briefs` était lisible avec la clé publique de l'app au moins depuis le 30/08/2026 ; deux messages contenant des adresses email y figuraient depuis le 11/09, et un message contenant le nom complet d'une cliente et des éléments de son programme depuis le 23/09. Fermée le 26/09 (vérifié de l'extérieur : réponse vide). Aucune trace d'accès n'est disponible ; la table est fermée depuis la v48. À noter dans le registre des violations ; une notification à la CNIL n'est pas obligatoire si le risque pour la personne est jugé faible.
- Suite du chantier : v49 suivi commercial (CHAUD / TIÈDE / FROID, raison, prochaine action, Signé / Perdu / Absent), puis funnel (Calendly) et P1 (interface prospect).
